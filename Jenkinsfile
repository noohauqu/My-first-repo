pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build step completed'
            }
        }
        stage('test') {
            steps {
                echo 'test step completed'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploy step completed'
            }
        }
    }
}
